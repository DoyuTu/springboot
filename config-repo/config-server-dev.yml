server:
  port: 37001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DoyuTu/springboot
          search-paths: config-repo
          username:
          password: